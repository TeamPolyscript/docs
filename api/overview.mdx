---
title: "Polyscript public HTTP API"
description: "Planned public HTTP API for Polyscript with endpoints for market data, order placement, strategy lifecycle, backtest jobs, and account management."
---

<Note>
  Public HTTP API for programmatic access. Endpoints will be documented here as they are designed and shipped.
</Note>

## Planned scope

- Market data (live + historical)
- Order placement
- Strategy lifecycle (deploy, pause, stop)
- Backtest jobs
- Account / billing

Auth via API keys (separate from the web session cookie). Spec pending.
